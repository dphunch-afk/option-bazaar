{
  "name": "option-bazaar",
  "private": true,
  "scripts": {
    "dev": "concurrently -k \"npm --prefix frontend run dev -- --host 0.0.0.0\" \"python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload\"",
    "frontend": "npm --prefix frontend run dev -- --host 0.0.0.0",
    "backend": "python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
